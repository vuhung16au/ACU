# Key Takeaways

- Compose builds UI with Kotlin functions instead of XML layouts.
- `@Composable` marks a function that draws part of the screen.
- `remember` keeps simple UI state while the screen stays alive.
- Material 3 gives you ready-made Android components such as `Button` and `Text`.
- Small composable functions are easier for beginners to read and reuse.
