# Key Takeaways

- `ViewModel` keeps important state outside the UI layer.
- `StateFlow` provides observable state for Compose screens.
- `collectAsState()` lets composables react to ViewModel updates.
- Keeping logic in the ViewModel makes the UI easier to test.
