# Key Takeaways

- `Scaffold` gives a clean Material layout structure.
- `TopAppBar`, `FloatingActionButton`, and `Card` are core Material 3 components.
- A custom color scheme helps the app feel consistent.
- Theme state can switch the UI between light and dark modes.
