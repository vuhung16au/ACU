# Key Takeaways

- Query shape is a performance feature.
- Projection usually beats loading full entities when the UI needs only a few columns.
- `AsNoTracking` removes change-tracking work for read-only pages.
- Filtering, sorting, and pagination belong in SQL when possible.
- `ToQueryString()` is a simple and powerful teaching tool.
