@page
@model PrivacyModel
@{
    ViewData["Title"] = "Data & Security Notes";
}
<h1>@ViewData["Title"]</h1>

<p>
    This classroom project uses an EF Core in-memory provider for fast LINQ practice.
    No external database credentials are required.
</p>

<ul>
    <li>Use this project for query experimentation and operator comparisons.</li>
    <li>Data resets when the app restarts because storage is in-memory.</li>
    <li>Use PostgreSQL projects for persistence-specific exercises.</li>
</ul>
