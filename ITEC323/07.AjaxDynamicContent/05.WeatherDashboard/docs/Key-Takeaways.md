# Key Takeaways

- Use mock data for dashboards without relying on external services.
- Polling with `setInterval()` can keep a dashboard current with simple client-side code.
- A single local endpoint can return enough data for several dashboard sections at once.
- Manual refresh buttons help students compare automatic and user-triggered updates.
- Loading and status messages make repeated data fetching easier to follow.
