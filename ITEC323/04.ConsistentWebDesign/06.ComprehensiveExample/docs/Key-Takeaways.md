# Key Takeaways

- Use layouts for page shells and partials for repeated UI fragments.
- Keep `_ViewStart.cshtml` at root for defaults and use folder-level overrides for sections.
- Page-level layout overrides are ideal for print/export screens.
- Typed models make partial views reusable and predictable.
- DRY architecture improves consistency and reduces maintenance cost.
