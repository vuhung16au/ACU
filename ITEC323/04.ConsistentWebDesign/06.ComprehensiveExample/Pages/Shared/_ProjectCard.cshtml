@model ProjectCardViewModel

<article class="card">
    <h3>@Model.Name</h3>
    <p>@Model.Summary</p>
    <p class="meta"><strong>Stack:</strong> @Model.Stack</p>
    <span class="tag">@Model.Status</span>
</article>