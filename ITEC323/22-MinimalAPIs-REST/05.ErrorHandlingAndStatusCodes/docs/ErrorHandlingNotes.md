# Error Handling Notes

Status codes help API clients understand what happened.

- `400` means the client sent invalid data.
- `404` means the requested resource was not found.
- `409` means the request conflicts with existing data.
- `500` means the server failed unexpectedly.
