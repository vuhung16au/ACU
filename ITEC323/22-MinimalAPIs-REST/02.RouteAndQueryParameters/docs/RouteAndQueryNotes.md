# Route And Query Notes

Use a route parameter when the value identifies one resource, such as `/api/tasks/2`.

Use a query string when the value changes how a list is filtered, such as `?priority=High`.

This project keeps the examples small on purpose so students can focus on how the URL is read by the endpoint.
