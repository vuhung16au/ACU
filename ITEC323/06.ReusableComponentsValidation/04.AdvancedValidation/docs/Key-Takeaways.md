# Key Takeaways

- `RegularExpression` is useful when input must match an exact pattern.
- `Compare` is useful for fields like password confirmation.
- Custom validators let you enforce rules such as minimum age.
- Razor encodes output by default, which helps prevent XSS.
- `@Html.Raw()` is dangerous for untrusted content.
- Sanitise rich text before rendering it as HTML.
