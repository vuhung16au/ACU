# Quick Start

## Prerequisites

- .NET 10 SDK installed

## Run The Project

```bash
cd 05.FileDataHandling
dotnet run
```

Open the local URL shown in the terminal.

## Pages To Check

- `/` explains the file handling examples
- `/Users` loads user records from CSV
- `/Create` validates and saves a new user

## Build Check

```bash
dotnet build
```
