@model PartialViews.Models.Product

<article class="product-card">
    <span class="badge rounded-pill mb-3">@Model.Category</span>
    <h3 class="h5">@Model.Name</h3>
    <p class="text-muted">@Model.Description</p>
    <div class="product-price">$@Model.Price.ToString("0.00")</div>
</article>
