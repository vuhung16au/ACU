# Key Takeaways

- Use partial views for repeated markup like headers, footers, cards, and form fields.
- Use view components when the UI needs C# logic before rendering.
- Keep validation rules on the model so create and edit forms share the same rules.
- Use a service layer for async CSV CRUD operations.
- Render user bios safely by default, and sanitise HTML before any trusted preview.
